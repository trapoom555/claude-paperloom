# Vault Log

Append-only. One line per action. Most recent at the bottom.

Format: `YYYY-MM-DD HH:MM | <action> | <target> | <note>`

Actions: `init`, `ingest`, `query`, `filter`, `lint`, `edit`.

---
